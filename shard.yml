name: collections
version: 0.2.5

authors:
  - Lillevang <jeppe.lillevang@gmail.com>

description: >
  A Crystal shard providing heap implementations such as MinBinaryHeap and MaxBinaryHeap.

dependencies: {}

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    branch: master # 1.6.4 (latest release) fails to build on Crystal >= 1.21; master supports it

crystal: '>= 1.14.0'

license: MIT
