module Collections::Graph::Algorithms
    # NOTE: For now just supports a graph with string vertices
    class TSP
        def self.find_shortest_path(graph : WeightedGraph(String), vertices : Array(String))
        end
    end
end